<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gxu.informationLibrary.dao.menuDao">
    <insert id="addMenu">
        insert into menu_info(menu_name, menu_level, father_menu, context_form)
        select #{menu.menu_name}, #{menu.menu_level}, menu_id, form_id
        from menu_info,
             form_info
        where menu_name = #{menu.menu_name}
          and form_name = #{menu.context_form};
    </insert>
    <delete id="deleteMenu">
        delete
        from menu_info
        where menu_id = #{menu};
    </delete>
    <select id="query" resultType="com.gxu.informationLibrary.entity.menuInfo">
        select menu_id,
               menu_name,
               menu_level,
               (select menu_name from menu_info where father_menu = menu_info.menu_id)  as father_menu,
               (select form_name from form_info where context_form = form_info.form_id) as context_form
        from menu_info;
    </select>
    <update id="editMenu">
        update menu_info
        set ${col_name}=#{value}
        where menu_id = #{menu_id};
    </update>
    <select id="getMenuById" resultType="com.gxu.informationLibrary.entity.menuInfo">
        select menu_id,
               menu_name,
               menu_level,
               (select menu_name from menu_info where father_menu = menu_info.menu_id)  as father_menu,
               (select form_name from form_info where context_form = form_info.form_id) as context_form
        from menu_info
        where menu_id = #{menu_id};
    </select>
    <resultMap id="menuSelectMap" type="Map">
        <result column="menu_name" property="label"/>
        <result column="menu_id" property="value"/>
    </resultMap>
    <select id="getMenuSelect" resultMap="menuSelectMap">
        select menu_name, menu_id
        from menu_info;
    </select>
</mapper>