<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gxu.informationLibrary.dao.dbManageDao">
    <update id="createDbDao">
        create database ${dbName};
        insert into db_info(db_name) values (#{dbName});
    </update>
    <update id="deleteDbDao">
        drop database if exists ${dbName};
        delete from db_info where db_name=#{dbName};
    </update>
    <select id="searchDbDao" resultType="dbInfo">
        select * from db_info where db_name like concat('%',#{dbName},'%')
    </select>
    <select id="getTables" resultType="int">
        select tables from db_info where db_name=#{dbName};
    </select>
</mapper>