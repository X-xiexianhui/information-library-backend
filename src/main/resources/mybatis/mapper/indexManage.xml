<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gxu.informationLibrary.dao.indexManageDao">
<select id="getColumns" resultType="String">
    select col_name from column_info where tb_id=(select tb_id from tb_info where db_name=#{db_name} and tb_name=#{tb_name});
</select>

</mapper>