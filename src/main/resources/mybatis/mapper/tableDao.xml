<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gxu.informationLibrary.dao.tableDao">
    <!--createTable方法-->
    <update id="createTable">
        create table ${tableName} (
            <foreach collection="configList" item="item" separator=",">
                ${item.filedName} ${item.filedType} ${filedConstraint},
            </foreach>
        )character set utf8 collate utf8_bin;
    </update>
<!--getPrimaryKey方法，用于设置外键约束-->
    <select id="getPrimaryKey">
        select Table_name,Column_name from information_schema.key_column_usage where Constraint_schema='information_library' and Constraint_name='PRIMARY';
    </select>
</mapper>